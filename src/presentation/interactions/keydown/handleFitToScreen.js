import { fitToScreen } from "../../../application/layout/utils/fitToScreen.js";

export function handleFitToScreen(svg, graph, graphHistory, gridManager) {
  fitToScreen(svg, graph, graphHistory, gridManager);
}
